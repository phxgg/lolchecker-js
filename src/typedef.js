/**
 * @typedef {import('node-fetch').Response} Response
 */

/**
 * @typedef {import('https-proxy-agent').HttpsProxyAgent<string>} HttpsProxyAgent
 */

/**
 * @typedef {Map<string, string>} Cookies
 */

/**
 * @typedef {object} Proxy
 * @property {string} host Hostname of the proxy
 * @property {string} port Port of the proxy
 * @property {string} [user] Username of the proxy
 * @property {string} [pass] Password of the proxy
 * @property {string} address Address of the proxy
 * @property {string} url URL of the proxy
 */

export const Types = {};
